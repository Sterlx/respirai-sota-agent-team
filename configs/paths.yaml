# Paths configuration for RespirAI
# Use environment variable ICBHI2017_ROOT to point to the dataset.
# Fallback for local development only.

dataset_root: "${ICBHI2017_ROOT}"
output_dir: "outputs"
checkpoint_dir: "checkpoints"